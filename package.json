{
  "name": "prelux-ecommerce",
  "version": "1.0.0",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm install && npm install --prefix backend && cd frontend && NODE_ENV=development npm install && npm run build && cd ..",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "mongodb": "^7.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}